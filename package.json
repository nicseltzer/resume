{
  "name": "career_path",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-html": "resume export artifacts/resume.html -t paper -F ",
    "build-pdf": "resume export artifacts/resume.pdf -t paper -F",
    "build": "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true PUPPETEER_EXECUTABLE_PATH=/opt/homebrew/bin/chromium npm run build-html && npm run build-pdf"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonresume-theme-paper": "^0.5.0",
    "resume-cli": "^3.0.5"
  }
}
